name: Separated Backend Build (Docker)

on:
  push:
    branches:
      - separated
    paths:
      - 'ip-cam-separated/backend/**'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: separated

      - name: Get version from package.json
        id: get_version
        run: |
          VERSION=$(jq -r '.version' ip-cam-separated/frontend/package.json)
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ip-cam-separated/backend
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/ip-cam-backend:latest
            ghcr.io/${{ github.repository_owner }}/ip-cam-backend:${{ steps.get_version.outputs.version }}

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.get_version.outputs.version }}
          name: "IP Cam Streamer v${{ steps.get_version.outputs.version }}"
          body: |
            ## 后端 Docker 镜像 v${{ steps.get_version.outputs.version }}
            
            ```bash
            docker pull ghcr.io/${{ github.repository_owner }}/ip-cam-backend:${{ steps.get_version.outputs.version }}
            ```
          draft: false
          prerelease: false
          append_body: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
