# MediaMTX 配置 - 统一容器版本
api: yes
apiAddress: :9997

# WebRTC 配置
webrtcAddress: :8889

# HLS 配置
hlsAddress: :8888

# RTSP 配置  
rtspAddress: :8554
readTimeout: 20s
writeTimeout: 20s

authMethod: internal
authInternalUsers:
  - user: admin
    pass: admin
    permissions:
      - action: api
      - action: read
      - action: publish

paths:
  all:
